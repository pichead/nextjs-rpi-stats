services:
  pi-stats:
    build:
      context: ./
    container_name: pi-stats
    ports:
      - '3000:3000'
    restart: always
    networks:
      - nginx-net

networks:
  nginx-net:
    driver: bridge
    name: "nginx-net"
    # external: true

